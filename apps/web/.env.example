# Frontend SPA agent backend URL.
# Production builds require an explicit non-localhost URL and do not permit the
# `?agent=` runtime override. Local `pnpm dev` may omit this and use localhost.
VITE_AGENT_ENDPOINT_URL=https://your-agent.example.com
