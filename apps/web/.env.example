# Public AInder agent backend URL.
#
# Required for production/preview builds. Do not rely on localhost fallbacks in
# shared environments; the SPA fails closed when this value is absent.
# Local development may use http://localhost:6790 or the ?agent= URL override.
VITE_AGENT_ENDPOINT_URL=http://localhost:6790
