# AInder agent backend URL.
#
# Required for production builds; production must not point at localhost.
# Leave blank in the checked-in example so production fails closed until the
# deploy platform injects a real public endpoint.
#
# Must be VITE_-prefixed so Vite exposes it to the browser bundle.
VITE_AGENT_ENDPOINT_URL=
